/**
 * Add px as unit to value.
 *
 * @param {Number} value
 */
export const toPx = value => {
   return `${value}px`;
};

/**
 * Add % as unit to value.
 *
 * @param {Number} value
 */
export const toPercentage = value => {
   return `${value}%`;
};
